/*
concursante que obtenga una 
puntuación igual o mayor que 
la puntuación del k -ésimo 
lugar avanzará a la siguiente ronda, 
siempre y cuando el concursante obtenga una punt
*/

#include <bits/stdc++.h>
using namespace std;
int main(){
    int n, k;
    cin>> n >> k;
    int arr[n];
    for(int i=0; i<n; i++){
        cin>>arr[i];
    }
    return 0;
}